#include <iostream>
#include <vector>

using namespace std;

// so, right now, we're going to use a vector of pairs, row and col to define the cells, yes
// i.e, now we also require a way to flatten the board for use to perform BFS on the dataset.
//
int main()
{
}